-- Create database (if not exists)
CREATE DATABASE nestjs_auth;

-- Use database
\c nestjs_auth;

-- User table will be created automatically by TypeORM
-- This script is just to ensure the database exists
